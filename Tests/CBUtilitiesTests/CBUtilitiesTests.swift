    import XCTest
    @testable import CBUtilities

    final class CBUtilitiesTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(CBUtilities().text, "Hello, World!")
        }
    }
